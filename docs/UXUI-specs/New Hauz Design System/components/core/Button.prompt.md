Primary action control for New Hauz — Montserrat label, 52px tall, 12px radius, 200ms transition. Orange `primary` is the conversion CTA; use it sparingly (one per view).

```jsx
<Button variant="primary">Ver Propiedades</Button>
<Button variant="ghost">Agenda una Asesoría</Button>
<Button variant="secondary" size="sm">Contactar Asesor</Button>
```

Variants: `primary` (orange CTA, glow shadow), `secondary` (navy), `ghost` (outline on light), `dark` (deep navy for premium sections), `link` (inline). Sizes: `sm` 40 · `md` 52 · `lg` 56. Props: `block`, `disabled`, `iconLeft`, `iconRight`.
