Hero property search bar (buscador inmobiliario): Operation · Type · Zone · Price segments with an orange BUSCAR button. White floating card meant to overlap the cinematic hero.

```jsx
<SearchBar onSearch={() => runSearch()} />
```

Best placed centered, ~−40px overlapping the hero bottom edge. On mobile the segments should stack (wrap the kit-level container).
